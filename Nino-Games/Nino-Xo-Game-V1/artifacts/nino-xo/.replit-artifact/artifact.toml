kind = "mobile"
previewPath = "/"
title = "Nino XO"
version = "1.0.0"
id = "artifacts/nino-xo"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 21142

[services.development]
run = "pnpm --filter @workspace/nino-xo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nino-xo", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/nino-xo", "run", "serve" ]

[services.env]
PORT = "21142"
BASE_PATH = "/"
